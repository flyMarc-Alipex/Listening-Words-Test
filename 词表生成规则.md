# WordCard 词表生成规则

> 本文件是生成、维护 WordCard 词表文件的统一规范。生成新词表时逐条对照执行。

## 一、文件格式

- 一个词表一个 `.txt` 文件，UTF-8 编码，放在 **`wordlists/`** 文件夹里即可被程序自动识别（右键菜单「词表」切换，`wordlists/words.txt` 为默认）。
- `#` 开头的行为注释，不影响加载。
- **每行一个单词**，格式：

```
英文单词 | 释义①(中文释义 + 英文释义) | 例句① | 释义② | 例句② …
```

### 规则要点

1. **字段分隔符是 `|`**，单词在最前。
2. **释义与例句成对出现**：`释义 | 例句` 为一组，多个义项依次排开，一对一。
3. **每个释义必须同时含中文释义和英文释义**（英文放括号里）。
4. **例句数量与释义数量严格一致**：几个义项就配几条例句，顺序一一对应，不允许少配或多配。
5. **多义词用 ① ② ③… 编号**；单义词一个释义时不编号。
6. 中文同义、近义并列用顿号「、」；义项之间用「① ② ③」区分，不再用分号堆在一行。
7. **例句是完整的英文短句**：句首大写、句尾句点，表达自然地道、贴近雅思听力语境（建议 15 词以内，不出现中文标点）。

### 示例

```
example | ① 例子、示例 (example, instance) | For example, many people now work from home. | ② 榜样 (role model) | She set a good example for the team.
practice | ① 练习、实践 (practice, training) | Regular practice is the key to improving your English. | ② 惯例、诊所、事务 (custom, medical practice) | It is normal practice to send a written follow-up.
memorize | 记忆、记住 (commit to memory) | I need to memorize these words before the test.
sentence | ① 句子 (a group of words forming a statement) | Write one sentence with each new word. | ② 判刑 (prison term) | He received a three-year prison sentence.
```

### 兼容的旧格式

`英文单词 | 中文释义`（无英文释义、无例句）仍可正常加载，视为单个义项；翻面时该义项若：
- 无例句 → 显示「例:(无例句)」
- 无释义 → 显示「(原文无提示)」

## 二、卡片上的显示效果

- 正面：英文单词。
- 翻面：每个义项独占一行（`① 中文释义 (English definition)`），例句以 `例①:… 例②:…` 编号逐行对应，与义项一一对上。

## 三、生成规范（从听力词表等源材料转换时）

1. **保持源材料顺序**（如 PDF 里的序号 1–N），不重排、不按字母排序。
2. **多个词表分开放、不合并**：一个 day/一份材料对应一个文件（如 `day1-words.txt`、`day2-words.txt`），命名即词表名，统一放在 `wordlists/` 目录下。
3. **释义以源材料已有中文提示为准**；材料未提供提示时，按本规则补全中文释义、英文释义和例句。
4. 已会记录与统计自动按词表名区分并存入 `data/` 目录：`words.txt` → `data/learned.txt` / `data/stats.txt`；其余 → `data/learned-<词表名>.txt` / `data/stats-<词表名>.txt`。

## 四、生成检查清单

- [ ] 每行一个单词，行尾无多余空格
- [ ] 注释行以 `#` 开头
- [ ] 每个释义包含中文 + 括号英文
- [ ] 例句数量 = 释义数量，顺序一一对应
- [ ] 多义词用 ①②③ 编号；单义词不编号
- [ ] 例句是完整英文短句（大写开头、句点结尾）
- [ ] 保留源材料顺序
- [ ] 多个词表分开保存，不合并